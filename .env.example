# Public URL of the backend API (baked into the frontend at image build time).
VITE_API_BASE_URL=https://api.example.com

# Host port exposed on the VPS (container serves HTTP on port 80 internally).
HTTP_PORT=8080
